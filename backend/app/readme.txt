文件夹说明：backend/app/
====================
本文件夹为后端主应用模块，采用分层架构组织代码，包含数据库、路由、服务、仓储等核心层。

子文件夹说明：
- bootstrap/：应用启动引导模块。
- core/：核心基础设施，包含数据库和仓储层。
- knowledge/：知识库管理模块。
- router/：API 路由层，定义所有 HTTP 接口。
- service/：业务逻辑服务层。

文件说明：
- scheduler.py：定时任务调度器。
- paths.py：路径工具，统一管理项目路径。
